[package]
name = "cpdb-sys"
version = "0.1.0"
edition = "2024"
description = "FFI bindings for cpdb-libs"
license = "MIT"
build = "build.rs"
links = "cpdb"

[dependencies]
libc = "0.2"
glib-sys = "0.22"
thiserror = "2.0"

[build-dependencies]
bindgen = "0.72"
pkg-config = "0.3"
